describe('StagingArea', () => {
  describe('getChangedFiles', () => {
    it('returns an array of ChangedFile objects whose contents in the working copy differ from the repository HEAD', () => {
      assert(true)
    })
  })
})
